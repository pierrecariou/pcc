const dates = document.querySelectorAll('.dates');

function selectDate() {
  const mois = document.querySelector('.mois.root');
  // jours.forEach((jour1) => {
  if (mois) {

  mois.click();
  }
};

export { selectDate };
