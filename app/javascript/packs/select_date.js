const dates = document.querySelectorAll('.dates');

function selectDate() {
  const semaine = document.querySelector('.root');
  // jours.forEach((jour1) => {
  if (semaine) {

  semaine.click();
  }
};

export { selectDate };
