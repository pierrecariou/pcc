<div class="card listed-article" data-sub-categories="<%= article.sub_categories.pluck(:name).join(" ") %>">
  <div class="user-info">
    <%= article.user.pseudo%>
  </div>
  <div class="article-comment">
    <%= article.description %>
  </div>
  <div class="article-preview">
    <p><%= article.title %></p>
    <p><%= article.source %></p>
    <p><%= article.site_description %></p>
    <img src="<%= article.image %>" width="240" height="135">
  </div>

  <div class="footer-card">
    <div class="footer-info">
    </div>
    <div class="footer-action">
      <%= link_to 'DÉBAT', article_path(article) %> UPVOTE
    </div>
  </div>
</div>
