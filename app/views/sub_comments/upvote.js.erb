function updateStarsCount(newCount) {
  console.log("yolo");
  const stars = document.querySelector("[data-sub-comment='<%= @sub_comment.id %>']");
  stars.innerHTML = newCount
}

updateStarsCount('<%= @sub_comment.stars %>')

